server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Brunocs1991/config-server-github.git
          default-label: main
          search-paths:
            - 'config-server*'